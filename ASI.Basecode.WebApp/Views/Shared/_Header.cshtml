@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
<header class="@((ViewBag.LoginView == true) ? "hide-navbar" : "")" style="background-color: #0D7C66; position: fixed;">
    <div class="logosec hover-pointer">
        <img src="~/img/exp1.png"
             style="width: 170px;" alt="logo"
             onclick="window.location.href = '@Url.Action("Index", "Home")';">
    </div>
     
   @*  <div class="logosec hover-pointer">
        <div class="d-flex align-items-center justify-content-center">
            <a asp-controller="Movie" asp-action="Index" class="btn btn-outline-primary">View Movie</a>
        </div>
    </div>
    <div class="logosec hover-pointer">
        <div class="d-flex align-items-center justify-content-center">
            <a asp-controller="Donation" asp-action="Index" class="btn btn-outline-primary">View Donation</a>
        </div>
    </div>
    <div class="logosec hover-pointer">
        <div class="d-flex align-items-center justify-content-center">
            <a asp-controller="Song" asp-action="Index" class="btn btn-outline-primary">View Songs</a>
        </div>
    </div> *@

    <div class="logosec" style="color: white">
        @HttpContextAccessor.HttpContext.Session.GetString("UserName")
        <div class="mr-20 hover-pointer" style="position: relative">
            <div class="dp">
                <img src="~/img/profile-logo.png"
                     class="dpicn hover-pointer"
                     alt="dp">
            </div>
            <div class="dropdown">
                <ul>
                    <li>
                        <i class="fa-solid fa-gear"></i>
                        <a href="#">Settings</a>
                    </li>
                    <li>
                        <i class="fa-solid fa-right-from-bracket"></i>
                        <a asp-action="SignOutUser" asp-controller="Account">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
</header>
